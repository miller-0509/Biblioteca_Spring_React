# URL base de la API del backend.
# En desarrollo usa el proxy de Vite (no hace falta cambiarla): /api
# En producción apunta al backend desplegado, por ejemplo:
#   VITE_API_URL=https://tu-backend.coolify.io
VITE_API_URL=/api
